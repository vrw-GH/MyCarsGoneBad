{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Welcome!
{% endblock %}

{% block tagLine %}
<span>
   My garage has over {{ cars|length *10}} cars 😍

   {# {% if numberOfCars>50 %} #}
   {% if cars|length>3 %}
   &emsp;(Thats a lot of cars !!)
   {% endif %}
</span>
{% endblock %}

{% block main %}
<div class="flex flex-col lg:flex-row ">

   {# {% set myCar= {id:1, name:"test", image:"s", statusString:"s", driver:"asd", class:"casd"} %} #}
   <aside data-controller="closeable" class="pb-8 lg:pb-0 lg:w-[auto] shrink-0 lg:block lg:min-h-fit text-white px-8
      border-b lg:border-b-0 lg:border-r border-white/20 transition-all overflow-hidden ">
      {{include('main/_carsListAside.html.twig')}}
   </aside>

   <section class="w-full">
      {% block carShow %}
      <div class=" text-2xl p-6">Please select a car</div>
      {# {{include('main/_carShow.html.twig')}} #}
      {% endblock %}
   </section>

</div>
{% endblock %}